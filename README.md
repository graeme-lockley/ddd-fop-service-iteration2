ddd-fop-service-iteration2
==========================

The next iteration of my DDD experiment where the FOP implementation is spun off into its own server and the FOP client does not change other than a different adaptor.
